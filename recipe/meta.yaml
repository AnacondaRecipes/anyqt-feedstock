{% set name = "AnyQt" %}
{% set version = "0.2.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name|lower }}-{{ version }}.tar.gz
  sha256: a9ed7c5169691a105ef2cea9114d6f8d439875b807d7258a2107f55cb56483f6
  patches:  # [osx]
    - patches/0001-patch-osx-path.patch  # [osx]

build:
  number: 0
  skip: True  # [py<36]
  script: {{ PYTHON }} -m pip install . --no-deps -vv --no-build-isolation

requirements:
  host:
    - pip
    - python
    - setuptools
    - wheel
  run:
    - python
    - pyqt

test:
  source_files:
    - tests
  imports:
    - AnyQt.QtCore
    - AnyQt.QtGui
    - AnyQt.QtWidgets
    - AnyQt.QtNetwork
    - AnyQt.QtSvg
  requires:
    - pip
    - pytest
  commands:
    - pip check
    # Fatal Python error: Aborted for Linux
    - pytest -v tests  # [not linux]

about:
  home: https://github.com/ales-erjavec/anyqt
  license: GPL-3.0-or-later
  license_family: GPL3
  license_file: LICENSE.txt
  summary: PyQt4/PyQt5 compatibility layer.
  description: |
    AnyQt exports a Qt5 compatible module namespace along with minimal renames 
    to support portability between versions and provides an optional compatibility import hook.
  doc_url: https://anyqt.readthedocs.io/en/stable
  dev_url: https://github.com/ales-erjavec/anyqt

extra:
  recipe-maintainers:
    - ales-erjavec
    - astaric
